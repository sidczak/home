{% extends 'base.html.twig' %}
{% block title %}{{title}}{% endblock %}

{% block body %}
    {#{{ show_source_code(_self) }}#}
        
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="mt-3">
                    <h2>Example 27</h2>
                </div>
            </div>
        </div>
    </div>
    <edu-app></edu-app>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <pre>
                    <code>
// assets/js/router/routes.js
{ 
    path: "/edu-user-details/:id", 
    component: EduUserDetails, 
    name: "userDetails",
    props: true,
    children: [
        { path: "edit", component: EduUserEdit, name: "userEdit", props: true }
    ]
}
                    </code>
                </pre>
                <pre>
                    <code>
// assets/js/edu-components/User/EduUserDetails.vue
import store from '../../store/store';
export default {
    name: "EduUsersDetails",
    store,
    props: ["id"],
    computed: {
        user() {
            return this.$store.getters.user( Number(this.id) )
        }
    }
}
                </pre>
                <pre>
                    <code>
// assets/js/edu-components/User/EduUserDetails.vue
import { mapMutations } from "vuex";

export default {
    name: "EduUserEdit",
    props: ["id"],
    computed: {
        user() {
            return this.$store.getters.user( Number(this.id) )
        }
    },
    methods: {
        ...mapMutations(["update"]),
        updateUser(e, type) {
            this.update({
                id: this.user.id,
                type: type,
                value: e.target.value
            })
        },
    }
};
                    </code>
                </pre>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}{% endblock %}
{% block javascripts %}{% endblock %}
